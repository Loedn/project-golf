$('#course-list').html(
  "<% @courses.each do |course| %><%= j render 'shared/course_card_draft', course: course %><% end %>"
)
$("body").animate({ scrollTop: window.pageYOffset + $(window).height()}, 600);
